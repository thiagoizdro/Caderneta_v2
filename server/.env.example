# Copie para .env e ajuste.
# `npm run db:dev` sobe um Postgres local nesta URL; com Docker, use a do docker-compose.yml (porta 5432).
DATABASE_URL=postgres://caderneta:caderneta@localhost:5433/caderneta
PORT=3001
